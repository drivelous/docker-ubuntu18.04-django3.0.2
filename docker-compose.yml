version: '3.3'

services:
  web:
    build: .
    volumes:
        - '.:/app:delegated'
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app/:/usr/src/app/
    ports:
      - 8000:8000
    stop_signal: SIGINT
    environment:
      PYTHONUNBUFFERED: 0
